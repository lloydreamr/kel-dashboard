/**
 * Skeleton Components
 *
 * Loading state components that match the layout of their corresponding
 * content components. Use skeletons instead of spinners for better UX.
 *
 * @example
 * import { CardSkeleton } from '@/components/skeletons';
 * {isLoading ? <CardSkeleton /> : <Card data={data} />}
 */

/** Base props for all skeleton components */
export type SkeletonProps = {
  /** Additional CSS classes */
  className?: string;
};

// Skeleton components will be added here as features are implemented
// Example: export { CardSkeleton } from './CardSkeleton';
