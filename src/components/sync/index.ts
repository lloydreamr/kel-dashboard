/**
 * Sync Components Module
 *
 * UI components for the sync engine, including conflict resolution.
 *
 * @see Story 8.4: Conflict Detection & Resolution
 */

export { ConflictDialog } from './ConflictDialog';
export type { ConflictDialogProps } from './ConflictDialog';
